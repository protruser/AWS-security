# Flask API가 Vite와 다른 호스트에서 서비스될 때만 지정합니다.
# 비워두면 개발 환경에서는 Vite /api 프록시를 사용합니다.
VITE_API_BASE_URL=

# npm run dev 실행 시 /api 프록시 대상. 기본값은 http://127.0.0.1:5000 입니다.
BACKEND_TARGET=http://127.0.0.1:5000
